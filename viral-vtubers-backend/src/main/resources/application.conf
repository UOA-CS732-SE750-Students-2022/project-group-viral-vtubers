ktor {
    development = true

    deployment {
        port = 8080
        port = ${?PORT}
        #watch = [com]
    }

    application {
        modules = [ com.viralvtubers.ApplicationKt.module ]
    }
}

mongodb {
    host = "localhost"
    port = "27017"
    databaseName = "virtual-vtubers"
    username = "admin"
    password = "password"
}

mongodb-test {
    host = "localhost"
    port = "27017"
    databaseName = "virtual-vtubers-test"
    username = "admin"
    password = "password"
}

